import NotFound from '../views/NotFound'

export const metadata = { title: 'Coming soon · Sumeet Group', robots: { index: false, follow: true } }

export default function NotFoundPage() {
  return <NotFound />
}
